name: Content Service CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [opened, synchronize, reopened]

# Avoid overlapping runs
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write
  pull-requests: write
  security-events: write
  packages: write

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
        
    - name: Run Unit Tests
      run: ./mvnw test -Dtest='!**/integration/**' -Dspring.profiles.active=test

  integration-tests:
    name: Integration Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
        
    - name: Run Integration Tests
      run: ./mvnw test -Dtest='**/integration/**' -Dspring.profiles.active=integration-test
      env:
        CI: true

  lint-quality:
    name: Lint & Quality Checks
    runs-on: ubuntu-latest
    needs: [unit-tests, integration-tests]
    permissions:
      contents: read
      security-events: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
        
    - name: Run SpotBugs (static analysis)
      run: ./mvnw spotbugs:check
      
    - name: Run Checkstyle (code style)
      run: ./mvnw checkstyle:check
      
    - name: Generate SpotBugs SARIF
      run: ./mvnw spotbugs:spotbugs -Dspotbugs.outputFormat=sarif -Dspotbugs.outputFile=spotbugs.sarif
      
    - name: Check if SARIF file exists
      run: |
        echo "Checking for spotbugs.sarif file..."
        ls -la spotbugs.sarif || echo "File not found"
        if [ -f spotbugs.sarif ]; then
          echo "File exists, size: $(wc -c < spotbugs.sarif) bytes" 
          head -5 spotbugs.sarif || echo "File is empty or unreadable"
        fi
      
    - name: Upload SpotBugs SARIF to GitHub
      if: always() && hashFiles('spotbugs.sarif') != ''
      uses: github/codeql-action/upload-sarif@v4
      with:
        sarif_file: spotbugs.sarif
        
    - name: Run Unit Tests and generate JaCoCo coverage
      run: ./mvnw -B -Dspring.profiles.active=test test jacoco:report
      
    - name: Run SonarCloud Analysis (use compiled classes and JaCoCo report)
      run: |
        ./mvnw -B sonar:sonar \
          -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml \
          -Dsonar.java.binaries=target/classes
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [unit-tests, integration-tests]
    permissions:
      contents: read
      security-events: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
        
    - name: Cache OWASP Dependency-Check data
      uses: actions/cache@v4
      with:
        path: ~/.m2/repository/org/owasp/dependency-check-data
        key: ${{ runner.os }}-dc-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-dc-
    
    - name: Configure Maven settings for OSS Index
      run: |
        mkdir -p ~/.m2
        cat > ~/.m2/settings.xml <<XML
        <settings>
          <servers>
            <server>
              <id>ossindex</id>
              <username>${OSSINDEX_USER}</username>
              <password>${OSSINDEX_TOKEN}</password>
            </server>
          </servers>
        </settings>
        XML
      env:
        OSSINDEX_USER: ${{ secrets.OSSINDEX_USER }}
        OSSINDEX_TOKEN: ${{ secrets.OSSINDEX_TOKEN }}
        
    - name: Run OWASP Dependency Check
      env:
        NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
      run: ./mvnw -Dnvd.api.key="${NVD_API_KEY}" org.owasp:dependency-check-maven:check
      
    - name: Upload Dependency-Check SARIF
      if: always() && hashFiles('target/dependency-check-report.sarif') != ''
      uses: github/codeql-action/upload-sarif@v4
      with:
        sarif_file: target/dependency-check-report.sarif
      
    - name: Upload OWASP Report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: owasp-dependency-check-report
        path: |
          target/dependency-check-report.html
          target/dependency-check-report.xml
        
    - name: Build (skip tests)
      run: ./mvnw -B -DskipTests package
      
    - name: Setup Snyk CLI
      uses: snyk/actions/setup@master
      
    - name: Snyk test (export SARIF)
      env:
        SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      run: |
        snyk auth "$SNYK_TOKEN"
        snyk test \
          --file=pom.xml \
          --package-manager=maven \
          --project-name=${{ github.repository }} \
          --severity-threshold=high \
          --sarif --sarif-file-output=snyk.sarif
      
    - name: Upload Snyk result to GitHub Code Scanning
      if: always() && hashFiles('snyk.sarif') != ''
      uses: github/codeql-action/upload-sarif@v4
      with:
        sarif_file: snyk.sarif

  docker-build:
    name: Build Docker Image & Scan Vulnerabilities
    runs-on: ubuntu-latest
    needs: [unit-tests, integration-tests, lint-quality, security-scan]
    permissions:
      contents: read
      packages: write
      security-events: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Cache Maven dependencies for Docker build
      uses: actions/cache@v4
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
        restore-keys: ${{ runner.os }}-maven-

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ghcr.io/${{ github.repository }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=sha
          type=raw,value=${{ github.sha }}
          type=raw,value=latest,enable={{is_default_branch}}
          type=raw,value=staging,enable=${{ github.ref == 'refs/heads/main' }}
          
    - name: Build and push Docker image
      id: build
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        build-args: |
          BUILDKIT_INLINE_CACHE=1
        secrets: |
          maven_repo=${{ github.workspace }}/.m2/repository
        
    - name: Run Trivy vulnerability scanner
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ghcr.io/${{ github.repository }}@${{ steps.build.outputs.digest }}
        format: 'sarif'
        output: 'trivy-results.sarif'
        
    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v4
      if: always() && hashFiles('trivy-results.sarif') != ''
      with:
        sarif_file: 'trivy-results.sarif'

    - name: Check if Trivy SARIF exists
      run: |
        echo "Checking for trivy-results.sarif file..."
        ls -la trivy-results.sarif || echo "File not found"
        if [ -f trivy-results.sarif ]; then
          echo "File exists, size: $(wc -c < trivy-results.sarif) bytes"
        fi

    - name: Upload Trivy SARIF as artifact
      uses: actions/upload-artifact@v4
      if: always() && hashFiles('trivy-results.sarif') != ''
      with:
        name: trivy-sarif
        path: trivy-results.sarif

    # Save image digest for production promotion
    - name: Save image digest
      run: echo "${{ steps.build.outputs.digest }}" > image-digest.txt

    - name: Upload image digest artifact
      uses: actions/upload-artifact@v4
      with:
        name: image-digest
        path: image-digest.txt
        retention-days: 30

  # production-deploy:
  #   name: Deploy to Production (DigitalOcean)
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 20
  #   needs: [unit-tests, integration-tests, lint-quality, security-scan, docker-build]
  #   if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name == 'pull_request' && github.head_ref == 'ci-cd_testing')
  #   permissions:
  #     contents: read
  #     packages: read
  #     deployments: write
  #   
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #     
  #   - name: Download image digest
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: image-digest
  #       path: .
  #       
  #   - name: Deploy to DigitalOcean
  #     uses: appleboy/ssh-action@v1.0.0
  #     with:
  #       host: ${{ secrets.DO_CONTENT_SERVICE_HOST }}
  #       username: ${{ secrets.DO_USERNAME }}
  #       key: ${{ secrets.DO_SSH_KEY }}
  #       script: |
  #         # Pull latest image
  #         docker pull ghcr.io/${{ github.repository }}:latest
  #         
  #         # Stop and remove old container
  #         docker stop yushan-content-service || true
  #         docker rm yushan-content-service || true
  #         
  #         # Start new container with production config
  #         docker run -d --name yushan-content-service --restart unless-stopped -p 8082:8082 \
  #           --log-driver=syslog \
  #           --log-opt syslog-address=udp://${{ secrets.LOGSTASH_HOST }}:${{ secrets.LOGSTASH_PORT }} \
  #           --log-opt tag="content-service" \
  #           --log-opt syslog-facility=local0 \
  #           -e SPRING_PROFILES_ACTIVE=default \
  #           -e DB_USERNAME=yushan_content -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} -e DB_NAME=yushan_content \
  #           -e DB_HOST=${{ secrets.CONTENT_DB_HOST }} -e DB_PORT=5432 \
  #           -e REDIS_HOST=${{ secrets.CONTENT_DB_HOST }} -e REDIS_PORT=6379 \
  #           -e SPRING_ELASTICSEARCH_REST_URIS=http://${{ secrets.CONTENT_DB_HOST }}:9200 \
  #           -e SPRING_KAFKA_BOOTSTRAP_SERVERS=${{ secrets.KAFKA_HOST }}:9092 \
  #           -e SPACES_ACCESS_KEY=${{ secrets.SPACES_ACCESS_KEY }} -e SPACES_SECRET_KEY=${{ secrets.SPACES_SECRET_KEY }} -e SPACES_ENDPOINT=${{ secrets.SPACES_ENDPOINT }} -e SPACES_BUCKET=${{ secrets.SPACES_BUCKET }} -e SPACES_REGION=sgp1 -e STORAGE_TYPE=s3 \
  #           -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
  #           -e EUREKA_CLIENT_SERVICEURL_DEFAULTZONE=http://${{ secrets.EUREKA_HOST }}:8761/eureka/ \
  #           -e CONFIG_SERVER_URI=http://${{ secrets.CONFIG_SERVER_HOST }}:8888 \
  #           -e EUREKA_INSTANCE_IP_ADDRESS=${{ secrets.DO_CONTENT_SERVICE_HOST }} -e EUREKA_INSTANCE_PREFER_IP_ADDRESS=true \
  #           ghcr.io/${{ github.repository }}:latest
  #         
  #         # Wait for service to start
  #         sleep 60
  #         
  #         # Health check with retry
  #         for i in {1..5}; do
  #           echo "Health check attempt $i/5..."
  #           if curl -f http://localhost:8082/actuator/health; then
  #             echo "Health check passed!"
  #             break
  #           else
  #             echo "Health check failed, retrying in 10 seconds..."
  #             sleep 10
  #           fi
  #         done
  #         
  #         # Final health check
  #         curl -f http://localhost:8082/actuator/health || exit 1
  #         
  #         echo "Content Service deployed successfully!"
  #         
  #   - name: Production Health Check
  #     run: |
  #       echo "Testing production deployment..."
  #       for i in {1..10}; do
  #         RESP=$(curl -fsSL -H "Accept: application/json" "http://${{ secrets.DO_CONTENT_SERVICE_HOST }}:8082/actuator/health" || true)
  #         echo "Attempt $i/10: $RESP"
  #         if echo "$RESP" | grep -q '"status":"UP"'; then
  #           echo "✅ Production health check passed!"
  #           exit 0
  #         fi
  #         sleep 10
  #       done
  #       echo "❌ Production health check failed!"
  #       exit 1
  #       
  #   - name: Notify deployment status
  #     if: always()
  #     run: |
  #       if [ "${{ job.status }}" == "success" ]; then
  #         echo "✅ Content Service deployed to production successfully!"
  #       else
  #         echo "❌ Content Service production deployment failed!"
  #       fi

  # dast-zap:
  #   name: OWASP ZAP Baseline (production)
  #   runs-on: ubuntu-latest
  #   needs: [production-deploy]
  #   if: needs.production-deploy.result == 'success'
  #   permissions:
  #     contents: read
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #   - name: ZAP Baseline
  #     uses: zaproxy/action-baseline@v0.13.0
  #     with:
  #       target: https://yushan.duckdns.org
  #       cmd_options: "-a -m 10 -J zap.json -w zap.md -r zap.html"
  #   - name: Upload ZAP report
  #     if: always()
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: zap-production
  #       path: |
  #         zap.html
  #         zap.json
  #         zap.md

  generate-reports:
    name: Generate CI Summary & Collect Reports
    runs-on: ubuntu-latest
    needs: [unit-tests, integration-tests, lint-quality, security-scan, docker-build]
    # needs: [unit-tests, integration-tests, lint-quality, security-scan, docker-build, production-deploy, dast-zap]
    if: always()
    permissions:
      contents: read
      checks: write
      security-events: write
    steps:
    - name: Download OWASP report
      uses: actions/download-artifact@v4
      if: always()
      with:
        name: owasp-dependency-check-report
        path: reports/owasp
      continue-on-error: true

    - name: Download Trivy SARIF
      uses: actions/download-artifact@v4
      if: always()
      with:
        name: trivy-sarif
        path: reports/trivy
      continue-on-error: true

    - name: Create empty Trivy report if missing
      run: |
        if [ ! -f reports/trivy/trivy-results.sarif ]; then
          echo "Creating empty Trivy report..."
          mkdir -p reports/trivy
          echo '{"version": "2.1.0", "runs": []}' > reports/trivy/trivy-results.sarif
        fi
      continue-on-error: true

    # - name: Download ZAP report
    #   uses: actions/download-artifact@v4
    #   if: always()
    #   with:
    #     name: zap-production
    #     path: reports/zap
    #   continue-on-error: true

    - name: Create CI summary
      if: always()
      run: |
        {
          echo "# Content Service – CI Reports";
          echo "- Commit: $GITHUB_SHA";
          echo "- Branch: ${GITHUB_REF##*/}";
          echo "- Event: ${{ github.event_name }}";
          echo "";
          echo "## Artifacts";
          if [ -f reports/owasp/dependency-check-report.html ]; then 
            echo "- OWASP Dependency-Check: reports/owasp/dependency-check-report.html"; 
          else 
            echo "- OWASP Dependency-Check: Not available (artifact not found or expired)"; 
          fi
          if [ -f reports/trivy/trivy-results.sarif ]; then 
            echo "- Trivy SARIF: reports/trivy/trivy-results.sarif"; 
          else 
            echo "- Trivy SARIF: Not available (artifact not found or expired)"; 
          fi
          # if [ -f reports/zap/zap.html ]; then 
          #   echo "- ZAP Security Report: reports/zap/zap.html"; 
          # else 
          #   echo "- ZAP Security Report: Not available (artifact not found or expired)"; 
          # fi
        } > summary.md

    - name: Upload consolidated report bundle
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: ci-reports
        path: |
          summary.md
          reports/**
